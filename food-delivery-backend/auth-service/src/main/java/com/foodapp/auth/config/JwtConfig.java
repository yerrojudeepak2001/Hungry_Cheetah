package com.foodapp.auth.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class JwtConfig {
    // Removed the old JwtTokenProvider bean since we now have our own updated implementation
}