server:
  port: 8082

spring:
  application:
    name: AUTH-SERVICE
  datasource:
    url: jdbc:mysql://100.122.144.43:3306/food_app
    username: teamuser
    password: password123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  cloud:
    config:
      enabled: false
  config:
    import: optional:configserver:http://configuser:configpassword@localhost:8888
  security:
    jwt:
      secret: ${JWT_SECRET:34510220d4bf1609c88acb0256b07a2e}
      expiration: 86400000 # 24 hours

# JWT Configuration - Format that matches the JwtTokenProvider expectations
jwt:
  secret: ${JWT_SECRET:34510220d4bf1609c88acb0256b07a2e}
  expiration: 86400000 # 24 hours

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: full
  circuitbreaker:
    enabled: true

resilience4j:
  circuitbreaker:
    instances:
      userService:
        minimumNumberOfCalls: 5
        failureRateThreshold: 50
        waitDurationInOpenState: 30s
  retry:
    instances:
      userService:
        maxRetryAttempts: 3
        waitDuration: 1s

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.foodapp.auth: DEBUG