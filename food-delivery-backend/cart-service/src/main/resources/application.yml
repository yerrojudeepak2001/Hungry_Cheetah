server:
  port: 8104

spring:
  application:
    name: CART-SERVICE
  datasource:
    url: jdbc:mysql://100.122.144.43:3306/food_app
    username: teamuser
    password: password123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
    config:
      enabled: false
  config:
    import: optional:configserver:http://localhost:8888
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: cart-service-group
  
  # Email Configuration
  mail:
    host: smtp.gmail.com
    port: 587
    username: hungrycheetah62@gmail.com
    password: feat seix sqbb lvhz
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com
  
  # ActiveMQ Configuration
  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin
    pool:
      enabled: true
      max-connections: 50
  
  # MongoDB Configuration
  data:
    mongodb:
      uri: mongodb://localhost:27017/food_app_cart

cart:
  expiry:
    inactive: 72 # hours
    abandoned: 24 # hours
  limits:
    max-items: 50
    max-quantity-per-item: 10
  features:
    save-for-later: true
    price-lock: 30 # minutes
  cleanup:
    schedule: "0 0 * * * *" # Every hour

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: full

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.foodapp.cart: DEBUG